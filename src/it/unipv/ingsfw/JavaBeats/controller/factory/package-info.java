/**
 * Provides classes implementing the Factory pattern to create objects without exposing the instantiation logic to the client.
 * This package includes factory classes responsible for creating instances of various objects based on input parameters or configuration.
 * It encapsulates the object creation process, promoting loose coupling and flexibility in object creation.
 */
package it.unipv.ingsfw.JavaBeats.controller.factory;